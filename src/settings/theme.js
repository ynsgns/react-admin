import {createTheme} from '@mui/material'

const theme = createTheme({
  palette: {
    primary: {
      main: '#2D2B90',
    },
    secondary: {
      main: '#0BAC73',
    },
    white: {
      main: '#FFF',
    },
  },
})

export default theme
